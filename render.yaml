services:
  - type: web
    name: vehicle-diagnostics-wai
    runtime: python311
    buildCommand: pip install -r requirements.txt
    startCommand: python3 wsgi.py
    envVars:
      - key: FLASK_ENV
        value: production
      - key: SESSION_SECRET
        generateValue: true
