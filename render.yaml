services:
  - type: web
    name: vehicle-diagnostics-wai
    runtime: python311
    buildCommand: pip install -r requirements.txt
    envVars:
      - key: FLASK_ENV
        value: production
      - key: SESSION_SECRET
        generateValue: true
